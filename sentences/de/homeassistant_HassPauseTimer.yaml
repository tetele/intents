language: de
intents:
  HassPauseTimer:
    data:
      - sentences:
          # pausieren
          - "pausiere[ (den|meinen|unseren)] Timer"
          - "pausiere[ (den|meinen|unseren)] <timer_start> Timer"
          - "pausiere[ (den|meinen|unseren)] Timer für <timer_start>"
          - "pausiere[ (den|meinen|unseren)] {area} Timer"
          - "pausiere[ (den|meinen|unseren)] Timer <area>"
          - "pausiere[ (den|meinen|unseren)] {timer_name:name} Timer"
          - "pausiere[ (den|meinen|unseren)] Timer (namens|für) {timer_name:name}"
          # pausieren / anhalten
          - "[(den|meinen|unseren) ]Timer (pausieren|anhalten)"
          - "[(den|meinen|unseren) ]<timer_start> Timer (pausieren|anhalten)"
          - "[(den|meinen|unseren) ] {area} Timer (pausieren|anhalten)"
          - "[(den|meinen|unseren) ]Timer <area> (pausieren|anhalten)"
          - "[(den|meinen|unseren) ]{timer_name:name} Timer (pausieren|anhalten)"
          - "[(den|meinen|unseren) ]Timer (namens|für) {timer_name:name} (pausieren|anhalten)"
          # halte ... an
          - "halt[e] (den|meinen|unseren) Timer an"
          - "halt[e] (den|meinen|unseren) <timer_start> Timer an"
          - "halt[e] (den|meinen|unseren) Timer für <timer_start> an"
          - "halt[e] (den|meinen|unseren) {area} Timer an"
          - "halt[e] (den|meinen|unseren) Timer <area> an"
          - "halt[e] (den|meinen|unseren) {timer_name:name} Timer an"
          - "halt[e] (den|meinen|unseren) Timer (namens|für) {timer_name:name} an"
