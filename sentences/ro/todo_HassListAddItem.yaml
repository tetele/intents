language: "ro"
intents:
  HassListAddItem:
    data:
      - sentences:
          - "<adauga> <item> (la|pe|<in>) <my_list>"
        response: item_added
        requires_context:
          domain: todo
        expansion_rules:
          my_list: "[lista[ mea] ]{name}"
          item: "{todo_list_item:item}"
