---
language: "nl"
intents:
  HassIncreaseTimer:
    data:
      - sentences:
          - "voeg <timer_duration> (toe;aan <my_timer>)"
          - "voeg [<in>] <area> <timer_duration> (toe;aan [<my>] <timer>)"
          - "<timer_duration> (toevoegen;aan <my_timer>)"
          - "[<in>] <area> <timer_duration> (toevoegen;aan [<my>] <timer>)"
          - "verleng (<my_timer>|<area_timer>) met <timer_duration>"
          - "aan (<my_timer>|<area_timer>) <timer_duration> toevoegen"
          - "(<my_timer>|<area_timer>) met <timer_duration> verlengen"
