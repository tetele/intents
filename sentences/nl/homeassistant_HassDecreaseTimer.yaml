---
language: "nl"
intents:
  HassDecreaseTimer:
    data:
      - sentences:
          - "haal <timer_duration> (af;van <my_timer>)"
          - "haal [<in>] <area> <timer_duration> (af;van [<my>] <timer>)"
          - "<timer_duration> (afhalen;van <my_timer>)"
          - "[<in>] <area> <timer_duration> (afhalen;van [<my>] <timer>)"
          - "verkort (<my_timer>|<area_timer>) met <timer_duration>"
          - "kort (<my_timer>|<area_timer>) in met <timer_duration>"
          - "(<my_timer>|<area_timer>) met <timer_duration> (verkorten|inkorten)"
